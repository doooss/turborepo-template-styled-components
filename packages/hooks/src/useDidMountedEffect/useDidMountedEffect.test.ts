import useDidMountedEffect from './useDidMountedEffect';

describe('useDidMountedEffect test', () => {
    it('useDidMountedEffect should be defined', () => {
        expect(useDidMountedEffect).toBeDefined();
    });
});
